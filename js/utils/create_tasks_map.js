export let tasks_array =[];



export function create_tasks_map(element) {
    tasks_array.map(function (element) {
        for (let i = 0; i <= tasks_array.length; i++) {
            return {
                i: element,
            }

        }
    });
}