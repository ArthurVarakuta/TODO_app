export let delete_task_buttons_array = [];


export function create_task_buttons_map(element) {
    delete_task_buttons_array.map(function (element) {
        for (let i = 0; i <= delete_task_buttons_array.length; i++) {
            return {
                i: element,
            }
        }
    });
}